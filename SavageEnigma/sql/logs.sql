CREATE TABLE "logs" (
    "ID" INT NOT NULL GENERATED ALWAYS AS IDENTITY 
                (START WITH 1, INCREMENT BY 1),
    "USERID" INT NOT NULL REFERENCES app."users"(ID),
    "ENCRYPTED_TEXT" VARCHAR(256),
    "DECRYPTED_TEXT" VARCHAR(256) NOT NULL,
    PRIMARY KEY (ID)
);